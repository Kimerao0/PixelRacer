import React from "react";
import { SiteSection } from "../../App";

const TeamsSection: React.FC<{
  setActiveSection: React.Dispatch<React.SetStateAction<SiteSection>>;
}> = ({ setActiveSection }) => {
  return <div>TeamsSection</div>;
};

export default TeamsSection;
